# Archived Lab — Lab 03 (legacy version)

## Lý do archive

Lab này được tạo trước khi roadmap chính thức được chốt.

Lab triển khai VNet trực tiếp bằng Terraform ngay từ đầu, bỏ qua bước Console và CLI. Điều này vi phạm triết lý học tập hiện tại:

```text
Console → CLI → Terraform → Production Design → Platform Engineering
```

Theo roadmap đã chốt, Lab 3 phải tách thành 3A (Console) và 3B (CLI) trước khi Terraform hoá ở Lab 4 — không nhảy thẳng vào Terraform.

## Lab chính thức thay thế

* [`lab-03a-vnet-networking-console`](../../lab-03a-vnet-networking-console) — đúng chuẩn 3A (Console) + 3B (CLI), Terraform hoá để dành cho Lab 4.

## Trạng thái

**Không tiếp tục phát triển thư mục này.** Giữ lại để tham khảo lịch sử (code Terraform ở đây vẫn hợp lệ về mặt kỹ thuật, chỉ sai về trình tự học tập), không xoá ngay — sẽ xoá hẳn sau khi xác nhận không cần đối chiếu nữa.

## Ngày archive

2026-06-19

